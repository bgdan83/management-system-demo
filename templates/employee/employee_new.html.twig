{% extends 'base.html.twig' %}

{% block title %}Add Employee{% endblock %}

{% block body %}
    <div class="container py-4">
    <h1>Add Employee</h1>
    
    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button name="new-employee" type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> Save
        </button>
    {{ form_end(form) }}
    </div>
{% endblock %}
